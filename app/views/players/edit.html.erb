<%=render 'application/header'%>

<div class="edit-page">
    <h3 class="form-header">Edit Player</h3> 

    <%=link_to "/players/#{@player.slug}/delete", :controller=>'players', :action=>'destroy' do%>
        <p>delete player profile</p>
    <%end%>
    
</div>

<%= render 'form'%>
<%=button_to "/players/#{@player.slug}/videos", :action=>'cancel', class: "cancel-player-button" do %>
        <p class="cancel-player">Cancel</p>
    <%end%>